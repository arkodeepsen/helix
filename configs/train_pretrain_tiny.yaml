# Ultra-fast sanity config (CPU-friendly)
dataset_paths:
  - data/processed/shard_0000
seq_len: 256

per_device_train_batch_size: 1
gradient_accumulation_steps: 2
learning_rate: 1.0e-3
weight_decay: 0.0
warmup_steps: 0
total_steps: 10
lr_scheduler_type: linear
bf16: false
fp16: false
gradient_checkpointing: false
max_grad_norm: 1.0
logging_steps: 1
save_steps: 0
save_total_limit: 1
num_workers: 0
